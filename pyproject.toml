[project]
name = "jet-guide"
version = "0.0.0"
description = "JET Encoding Guide"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "zensical==0.0.57",
]

[dependency-groups]
dev = [
    "mike",
]

[tool.uv.sources]
mike = { git = "https://github.com/squidfunk/mike.git", rev = "2.2.0+zensical-0.1.0" }

[tool.uv]
package = false
